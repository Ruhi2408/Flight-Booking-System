package com.app.FlightSystem.Service;

import java.time.LocalDate;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.app.FlightSystem.model.Flight;
import com.app.FlightSystem.repository.FlightRepository;

@Service
public class FlightService {
	@Autowired
	FlightRepository flightRepository;
	
	public List<Flight> searchFlights(String sourcee, String destination,LocalDate departureDate){
		return flightRepository.findBySource_locationAndDestinationAndDepartureDate(sourcee, destination, departureDate);
	}
	public Flight getFlightbyId(String id,LocalDate departureDate) {
		return flightRepository.findById(id).orElse(null);
	}
	
	public Flight addFlight(Flight flight) {
		return flightRepository.save(flight);
	}

}
